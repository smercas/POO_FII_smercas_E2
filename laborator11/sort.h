#ifndef LABORATOR11_SORT_H
#define LABORATOR11_SORT_H

#include <cstdint>

template<typename T>
Class Sort {
    static bool sort(T* values, const uint64_t count) {
        return false;
    }
    Sort() = deleted;
};

#endif //LABORATOR11_SORT_H
